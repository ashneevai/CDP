# Strict identity replay supersession note

Historical candidate cohorts and first-review annotations remain immutable and available for
audit. In particular, the earlier 140-page cohort was produced under router policy `3.0`; its
status for canonical claim-form classification comparisons is:

`SUPERSEDED_BY_STRICT_FORM_IDENTITY_GATE`

This status does not delete, rewrite, relabel, or otherwise mutate the historical review data.
It records that those results did not use the `strict-form-identity-v1` policy and must not be
presented as results of the strict identity gate.